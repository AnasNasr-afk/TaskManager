// TaskManagerUITests/TaskManagerUITests.swift
import XCTest

final class TaskManagerUITests: XCTestCase {
    override func setUp() {
        continueAfterFailure = false
    }

    func testExample() {
        // Keep at least one simple test so the target isn't empty
        XCTAssertTrue(true)
    }
}

